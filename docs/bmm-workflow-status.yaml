# Workflow Status Template

# This tracks progress through BMM methodology Analysis, Planning, and Solutioning phases.
# Implementation phase is tracked separately in sprint-status.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: '2025-11-15'
project: 'proyecto-nixtia'
project_type: 'software'
selected_track: 'method'
field_type: 'greenfield'
workflow_path: 'method-greenfield.yaml'
workflow_status: |
  # Phase 0: Discovery (Optional)
  brainstorm-project: docs/discovery/brainstorming-session-results-2025-11-15  # agent: analyst
  research: skipped  # agent: analyst
  product-brief: docs/discovery/product-brief-proyecto-nixtia-2025-11-15 19-16-57-035  # agent: analyst

  # Phase 1: Planning
  prd: docs/planning/PRD  # agent: pm
  validate-prd: completed  # agent: pm
  create-design: docs/planning/ux-design-specification.md  # agent: ux-designer (conditional: if_has_ui)

  # Phase 2: Solutioning
  create-architecture: docs/planning/architecture  # agent: architect
  create-epics-and-stories: required  # agent: pm (REQUIRED: final epic breakdown with full context)
  test-design: recommended  # agent: tea (recommended)
  validate-architecture: optional  # agent: architect
  implementation-readiness: required  # agent: architect (REQUIRED: final gate check)

  # Phase 3: Implementation
  sprint-planning: required  # agent: sm
